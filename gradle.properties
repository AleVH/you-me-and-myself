# Increase Gradle daemon heap & metaspace (safe defaults)
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dkotlin.daemon.heap.size=2048m

# (Optional) faster builds
org.gradle.parallel=true
org.gradle.configureondemand=true
